venv/
temp_venv/
__pycache__/
.pytest_cache/
.mypy_cache/
.vscode/
.idea/
*.pyc
*.pyo
*.pyd
*.log
dist/
build/
.DS_Store
*.egg-info/
.env
.env.prod
.coverage
tests/data/
tests/images/
/models/*
!/models/README.md
/models_backup/

# P0 security artifacts
# Self-signed dev TLS certs (см. infrastructure/nginx/generate_self_signed.sh)
certs/
# FAISS persist хранит plaintext-биометрию — не коммитим
*.index
faiss_meta.pkl
faiss_meta.json

# P1 evaluation harness: кеш эмбеддингов и отчёты (генерируются, не коммитятся)
evaluation/cache/
evaluation/cache_glintr100/
evaluation/out/

# P2 liveness eval-harness: кеш предсказаний и отчёты
evaluation/liveness/cache/
evaluation/liveness/out/

# P3 LFW eval-harness: кеш эмбеддингов и отчёты
evaluation/lfw/cache/
evaluation/lfw/out/

# Локальные eval-датасеты (скачиваются/генерируются локально, не в git)
Face Data/
Extracted Faces/
Anti-Spoofing Dataset/
lfw_data/

# Benchmark outputs (capture_lb_capacity_curve и др. — генерируются локально)
benchmarks/

# Одноразовый шум нагрузочных тестов и профилировки в корне
k6_*.json
k6_*.txt
k6_*.csv
summary_*.json
summary_*.txt
cpu_samples_*.csv
metrics_after_*.txt
*.pid
run_*_after_patch.ps1
run_short_path_diag.ps1
series_worker_since_*.log
queue_*.log
workers_*.log
temp_upload_test.py
test.json
summary.json

# Локальные настройки Claude Code (permissions и т.п. — не коммитятся)
# settings.json (shared) может коммититься; settings.local.json — нет
.claude/settings.local.json
# Локальные определения агентов/память/сессии ассистента — не публикуются
# (создаются через /agents, не запрашивались для шаринга в команде)
.claude/agents/
.claude/agent-memory/
.claude/memory/
.claude/projects/

# 152-ФЗ: реальные фото лиц (биометрия) из локальных камер — НЕ коммитить.
# Папки Camera Roll/ экспортируются Windows-камерой и содержат ПДн.
Camera Roll/
Camera Roll*/
*.Pro.jpg
*.Pro.png

# Локальная инструкция ассистента (не публикуется)
CLAUDE.md